<h1>Portfolio Items</h1>

<% @works_item.each do |work| %>
  <h2><%= work.title %></h2>
  <h3><%= work.subtitle %></h3>
  <p><%= work.body %></p>
  <%= image_tag work.thumb_image unless work.thumb_image.nil? %>
<% end %>
